{
  "name": "acceptance-playwright",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright-BDD acceptance lane: runs @e2e (live UI) and @component (stubbed UI) Gherkin scenarios from specs/ against the web app.",
  "scripts": {
    "sync": "node sync-specs.mjs",
    "gen": "npm run sync && bddgen",
    "pretest": "npm run gen",
    "test": "playwright test",
    "list": "npm run gen && playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "~1.55.0",
    "playwright-bdd": "^8.3.1"
  }
}
